# v22 Test

1. Run `run_studio_v22.bat`.
2. Click toolbar `Next Task`.
3. Expected:
   - If voice is missing: Universal Production Dialog opens in voice mode.
   - If image is missing: image import mode.
   - If music is missing: music import mode.
4. In Control -> Production Queue, click `Do Next Task`.
5. Expected same universal dialog behavior.
6. After approve/import, queue/tree/progress should update.
